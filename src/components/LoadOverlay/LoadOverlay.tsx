import React from 'react';
import {Loader} from './style';

const LoadOverlay = () => {
    return (
        <Loader>
            <p>Get ready to scan the QR code that appears</p>
        </Loader>
    );
};

export default LoadOverlay;